config_planning:
  v_lon_desired: 15.5

  # this can either be "CART" or "CVLN"
  coordinate_system: "CVLN"
  # this can either be "CENTER" or "REAR"
  reference_point: "CENTER"

config_vehicle:
  ego:
    # velocity in m/s
    v_lon_min: 0.0
    v_lon_max: 33.3
    v_lat_min: -4.0
